{% extends 'base.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>NomPromotio</th>
                <td>{{ promotion.nomPromotio }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ promotion.description }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ promotion.dateDebut ? promotion.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ promotion.dateFin ? promotion.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ promotion.pourcentage }}</td>
            </tr>
        </tbody>
    </table>

<div class="btn-group" role="group" aria-label="Actions">
                <a class="btn btn-primary" href="{{ path('app_promotion_index') }}">Retour a liste </a>
                <a class="btn btn-warning" href="{{ path('app_promotion_edit', {'id': promotion.id}) }}">Modif</a>
</div>
    {# <a href="{{ path('app_promotion_index') }}">back to list</a> #}

    {# <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}">edit</a> #}

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
