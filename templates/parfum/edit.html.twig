{% extends 'base.html.twig' %}

{% block title %}Edit Parfum{% endblock %}

{% block body %}
    <h1>Edit Parfum</h1>

    {{ include('parfum/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_parfum_index') }}">back to list</a>

    {{ include('parfum/_delete_form.html.twig') }}
{% endblock %}
